const DIRECTORIES = "/auth";
export enum AUTH_LINKS_ENUM {
  SIGNIN_PAGE = DIRECTORIES + "/signin",
  SIGNUP_PAGE = DIRECTORIES + "/signup",
  NEW_PASSWORD_PAGE = DIRECTORIES + "/new-password",
  FORGOT_PASSWORD_PAGE = DIRECTORIES + "/forgot-password",
  FORGOT_VERIFICATION_PAGE = DIRECTORIES + "/verification",
}
